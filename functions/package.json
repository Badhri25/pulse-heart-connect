{
  "name": "pulsepod-functions",
  "private": true,
  "engines": { "node": "18" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.4.1",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
