[build]
  command = "npm run build"
  publish = "docs"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Optional: SPA fallback if you switch to BrowserRouter later
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
